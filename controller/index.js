/**
 * @index js
 */

const userRegister = require('./register');
const userLogin = require('./login');
const userDisplay = require('./display');

module.exports = {
  userRegister,
  userLogin,
  userDisplay,
};
